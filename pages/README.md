# Custom pages

Add a standalone SEO HTML page as `pages/your-page-slug.html`. The build preserves the source HTML, extracts the first image for a thumbnail, keeps keywords/schema metadata, and publishes the page at `/your-page-slug`. Existing site routes take priority if a slug conflicts with one of them.