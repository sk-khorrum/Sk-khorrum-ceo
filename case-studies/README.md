# Case studies

Add a case study as `case-studies/project-slug.html` to use the full SEO HTML flow (title, description, first-image thumbnail, body, keywords, and JSON-LD). Metadata-only JSON files remain supported; only publish verified results. Detail pages publish at `/case-studies/project-slug` and appear automatically in `/case-studies`.