# Projects

Add a project as `projects/project-slug.html` to use the full SEO HTML flow (title, description, first-image thumbnail, body, keywords, and JSON-LD). Metadata-only `projects/project-slug.json` files are also supported; use `url` for the external live project URL. Project detail pages publish at `/projects/project-slug` and appear automatically in `/portfolio`.