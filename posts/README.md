# Blog posts

Add a new post as `/posts/your-post-slug.html`. The build scans this folder automatically and creates `public/generated/posts.json` and sitemap entries. Include a `<title>`, an optional description meta tag, and semantic article HTML. Run `npm run build`; no React component changes are needed.